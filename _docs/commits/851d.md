# 851d Update: use namespaced MetaFramework translations

## Summary

- Use the MetaFramework translation namespace in dictionary controllers and views.
- Require MetaFramework 2.x, where translations are loaded directly from the package.
- Preserve application-level overrides through Laravel's `lang/vendor/mfw` convention.

## Verification

- `composer validate --strict`
- `php vendor/bin/pint --dirty --format agent`
- `php vendor/bin/phpunit` (12 tests, 46 assertions)
